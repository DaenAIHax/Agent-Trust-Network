{{/*
The in-cluster Postgres uses a StatefulSet with `volumeClaimTemplates` so
the PVC is rendered automatically per replica with a stable name.

This file is intentionally left empty: defining a standalone PVC alongside
the StatefulSet would shadow the per-replica claim and break upgrades.

If you need to inject extra storage (e.g. a backup volume) add it here as
a {{ "{{- if .Values.postgres.internal }}" }} guarded PVC.
*/}}
